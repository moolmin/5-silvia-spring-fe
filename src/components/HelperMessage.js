import React from 'react'

function HelperMessage({text}) {
    return (

        <div className="HelperContainer">
            <div className="HelperMessage">{text}</div>
        </div>
    )
}

export default HelperMessage
